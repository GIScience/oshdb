package org.heigit.ohsome.oshdb.util.tagtranslator;

/**
 * An OSM tag ({@link OSMTag}) or an OSM tag key ({@link OSMTagKey}).
 *
 * <p>This interface allows to write methods that accept both OSMTag and OSMTagKey
 * objects as parameters without overloading, and it allows to write methods that
 * return either an OSMTag or an OSMTagKey object.</p>
 */
public interface OSMTagInterface {}
